<Page
    x:Class="AlgoDrawUwp.Pages.BubleSort"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AlgoDrawUwp.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Custom="using:AlgoDrawUwp"
    mc:Ignorable="d"
    Background="{x:Null}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="231*"/>
            <RowDefinition Height="62"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="151"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button Content="Button" Margin="10" VerticalAlignment="Top" Height="42" Width="131" Click="Button_Click" HorizontalAlignment="Center" Grid.Row="1" />

        <Custom:MassViev x:Name="mass" Grid.Column="1"/>

        <TextBlock Grid.Column="2" x:Name="code"/>

        <Slider Grid.Column="1" Grid.Row="1" Margin="0,31,0,0" Minimum="500" Maximum="2000" Value="1000" x:Name="timeSlider"/>
        <TextBlock Grid.Column="1" Grid.Row="1" Margin="0,0,0,31" Text="Задержка(мс)"/>

    </Grid>
</Page>
